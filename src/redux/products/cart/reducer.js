// import actions from './actions'
export default (state = {}, action) => {
    switch (action.type) {
        case "":
            return state
            
        default:
            return state;
    }
};